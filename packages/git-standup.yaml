name: git-standup
version: "2.3.2" # repo: nilbuild/git-standup
url: https://raw.githubusercontent.com/nilbuild/git-standup/refs/tags/{{ version }}/git-standup
move_rules:
  - src_regex: git-standup
    dst: /usr/libexec/git-core/git-standup
    mode: 0755
