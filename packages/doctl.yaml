name: doctl
url: https://github.com/digitalocean/doctl/releases/download/v{{ version }}/doctl-{{ version }}-linux-{{ deb_architecture }}.tar.gz
version: "1.162.0" # repo: digitalocean/doctl
type: release_asset
move_rules:
  - src_regex: doctl.*
    dst: /usr/local/bin/doctl
    mode: 0755
